
    {{ form_start(form) }}
    <div class="form-group">
        <strong>Nom de la sortie :</strong>
        {{ sortie.nom }}

    </div>
    <div class="form-group">
        <strong>Date et heure de début :</strong>
        {{ sortie.dateHeureDebut|date('d/m/yy') }}

    </div>
    <div class="form-group">
        <strong> Campus :</strong>
        {{ sortie.campus }}

    </div>
    <div class="form-group">
        <strong>  Lieu :</strong>
        {{ sortie.lieu }}

    </div>
    <div class="form-group">
        <strong> Motif d'annulation :</strong>
        {{ form_widget(form.infosSortie, {'attr':{'class':'form-control'}})}}
    </div>
    <input type = "submit" class="btn btn-primary">
    {{form_end(form) }}

